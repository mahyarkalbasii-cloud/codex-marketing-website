# Public endpoint for demo request submissions, for example an n8n or form backend webhook.
# Required in production; without it the form shows a fallback contact error instead of fake success.
NEXT_PUBLIC_DEMO_REQUEST_ENDPOINT=
